import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, select, boolean } from "@storybook/addon-knobs";
import Button from './'
import { MdDelete, MdErrorOutline } from 'react-icons/md';
import { SeparateElements } from '../DocsStyle'

<Meta title="Button" component={<Button />} />

# Button

Button is a component which supports different kind of types, starting from `color`, `size`, `modifiers` etc.

```jsx
import { Button } from 'finki-ui';

const Page = () => (
  <Button />
)
```

### Props
<Props of={Button} />

### Default Button
<Preview withSource="open">
  <Story name="default">
    <div>
        <Button>Enroll</Button>
    </div>
  </Story>
</Preview>

### Buttons with different sizes
<Preview withSource="open">
  <Story name="sizes">
    <SeparateElements>
      <Button sizeType="xs">Button XS</Button> <br />
      <Button sizeType="sm">Button SM</Button> <br />
      <Button sizeType="md">Button MD</Button> <br />
      <Button sizeType="lg">Button LG</Button> <br />
      <Button fullWidth>Button Full Width</Button> <br />
    </SeparateElements>
  </Story>
</Preview>

### On hover Buttons
<Preview withSource="open">
  <Story name="hover">
    <SeparateElements>
        <Button>No Modifier</Button> 
        <Button buttonModifiers={['hoverInfo']}>Hover Info</Button> 
        <Button buttonModifiers={['hoverDanger']}>Hover Danger</Button> 
        <Button buttonModifiers={['hoverWarning']}>Hover Warning</Button> 
        <Button buttonModifiers={['hoverSuccess']}>Hover Success</Button>
      </SeparateElements>
  </Story>
</Preview>

### Different types of Buttons
<Preview withSource="open">
  <Story name="types">
      <SeparateElements>
        <Button buttonModifiers={['buttonInfo']}>Button Info</Button>
        <Button buttonModifiers={['buttonPrimary']}>Button Primary</Button> 
        <Button buttonModifiers={['buttonWarning']}>Button Warning</Button> 
        <Button buttonModifiers={['buttonSuccess']}>Button Success</Button>
        <Button buttonModifiers={['buttonDanger']}>Button Danger</Button> 
        <Button disabled>Button Disabled</Button>
        <Button buttonModifiers={['buttonLink']}>Button Link</Button>
      </SeparateElements>
  </Story>
</Preview>

### Buttons with icons
<Preview withSource="open">
  <Story name="with icons">
    <SeparateElements>
        <Button fullHeight icon={<MdDelete />} buttonModifiers={['buttonDanger']} />
        <br />
        <Button buttonModifiers={['buttonDanger']} icon={<MdErrorOutline />} iconPosition="right">
          Button with Icon on the right!
        </Button>
        <br />
        <Button buttonModifiers={['buttonDanger']} icon={<MdErrorOutline />}>
          Button with Icon on the left!
        </Button>
        <br />
        <Button icon={<MdDelete />}>Button with Custom Icon</Button>
        <Button icon={<MdDelete color="red" />}>Button with Custom Icon Colored</Button>
    </SeparateElements>
  </Story>
</Preview>