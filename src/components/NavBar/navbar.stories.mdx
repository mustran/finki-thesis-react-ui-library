import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Navbar from './'
import { IoMdAnalytics } from 'react-icons/io';
import { MdPhotoLibrary, MdPeople } from 'react-icons/md';

<Meta title="Navbar" component={<Navbar/>} />

# Navbar

The Navbar is a horizontal navigation bar component. It takes an image element as the 
`logo` icon at the top. Menu items are rendered as children of the Navbar using the Navbar.MenuItem component. 
The MenuItem takes an `icon` as a prop (as a React Node), and an `active` prop, 
which indicates whether the selected menu item is active. `onClick` is an optional prop which lets you add 
custom click logic to the MenuItem. The MenuItem can in an `a` or `Link` from react-router element as well, 
just make sure to clear the text-decoration.

```jsx
import { Navbar } from 'finki-ui';

const Page = () => (
  <Navbar />
)
```

### Props

<Props of={Navbar}/>

### Navbar example
<Preview withSource="open">
  <Story name="navbar">
    <div style={{height: '600px'}}>
        <Navbar logo="https://www.finki.ukim.mk/Content/dataImages/downloads/logo-large-500x500_2.png">
            <Navbar.MenuItem
            icon={<IoMdAnalytics size="192x192" />}
            onClick={() => console.log('iKnow clicked')}
            active
            >
            iKnow
            </Navbar.MenuItem>
            <Navbar.MenuItem
            icon={<MdPhotoLibrary size="192x192" />}
            onClick={() => console.log('Archive clicked')}
            >
            Archive
            </Navbar.MenuItem>
            <Navbar.MenuItem
            icon={<MdPeople size="192x192" />}
            onClick={() => console.log('Students clicked')}
            >
            Students
            </Navbar.MenuItem>
        </Navbar>
    </div>
  </Story>
</Preview>